{
  "name": "web-app-manifest-setup",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build:minimal": "sh build.sh minimal-ui",
    "build:standalone": "sh build.sh standalone",
    "build:fullscreen": "sh build.sh fullscreen"
  }
}
